(() => {
  const q=(s,r=document)=>r.querySelector(s),qa=(s,r=document)=>[...r.querySelectorAll(s)];
  const read=(k,d)=>{try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(d))}catch{return d}};
  const write=(k,v)=>localStorage.setItem(k,JSON.stringify(v));
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
  const toast=s=>window.showToast?.(s);
  const PROFILE='aii-local-profile',BRAND='aii-local-brand',STYLES='aii-local-styles',IDEAS='aii-local-ideas';
  const defaults={profile:{displayName:'Karolina',handle:'@karolajna.86',niche:'Beauty / Lifestyle',bio:'Budżetowe beauty, testy i praktyczne rekomendacje',platform:'Instagram'},brand:{tone:'Naturalny, potoczny i konkretny',promise:'Pomagam wybierać rozsądniej i bez przepłacania',pillars:'Testy produktów\nBudżetowe alternatywy\nPorównania i rankingi\nPraktyczne wskazówki',avoid:'Przesadzone obietnice\nFałszywa pilność\nNiezweryfikowane dane'}};
  const style=document.createElement('style');style.textContent=`
  .core-local{display:grid;gap:12px}.core-card{padding:15px;border:1px solid #e7e9f1;border-radius:15px;background:#fff}.core-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.core-grid label{font-size:7px;color:#6f7684;display:grid;gap:5px}.core-grid input,.core-grid textarea,.core-grid select{width:100%;box-sizing:border-box;border:1px solid #dfe2eb;border-radius:10px;padding:9px;background:#fafafe;font:inherit;color:#262933}.core-actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:10px}.core-pill{display:inline-flex;padding:4px 7px;border-radius:999px;background:#edf8f1;color:#287a4b;font-size:6.8px;font-weight:900}.core-list{display:grid;gap:7px;margin-top:10px}.core-row{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:8px;align-items:center;padding:9px;border:1px solid #eceef5;border-radius:10px;background:#fafafe;font-size:8px}.core-row small{display:block;color:#7a8190;margin-top:3px}.core-colors{display:flex;gap:8px;flex-wrap:wrap}.core-swatch{width:50px;height:50px;border-radius:12px;border:1px solid #ddd;display:grid;place-items:end center;padding:4px;font-size:6px;background:var(--sw)}.core-help{line-height:1.6;font-size:8px;color:#626a78}.core-check{display:grid;gap:7px;margin-top:8px}.core-check div{padding:9px;border-radius:10px;background:#fafafe;border:1px solid #eceef5;font-size:8px}@media(max-width:760px){.core-grid{grid-template-columns:1fr}}
  `;document.head.appendChild(style);
  const input=(id,label,val,type='text')=>`<label>${label}${type==='textarea'?`<textarea id="${id}" rows="4">${esc(val)}</textarea>`:`<input id="${id}" value="${esc(val)}">`}</label>`;

  function profileView(){const p={...defaults.profile,...read(PROFILE,{})};return `<section class="core-local"><div class="core-card"><div class="section-head"><div><h2>Profil i wizerunek</h2><p class="page-subtitle">Lokalne źródło prawdy dla generatorów, planera i raportów.</p></div><span class="core-pill">0 TOKENÓW</span></div><div class="core-grid">${input('cpName','Nazwa / podpis',p.displayName)}${input('cpHandle','Nick',p.handle)}${input('cpNiche','Nisza',p.niche)}${input('cpPlatform','Główna platforma',p.platform)}${input('cpBio','Bio / opis profilu',p.bio,'textarea')}</div><div class="core-actions"><button class="primary" id="cpSave">Zapisz profil</button></div></div></section>`}
  function brandView(){const b={...defaults.brand,...read(BRAND,{})};return `<section class="core-local"><div class="core-card"><div class="section-head"><div><h2>Marka osobista</h2><p class="page-subtitle">DNA marki używane przez lokalne generatory.</p></div><span class="core-pill">OFFLINE READY</span></div><div class="core-grid">${input('cbTone','Ton komunikacji',b.tone)}${input('cbPromise','Obietnica marki',b.promise)}${input('cbPillars','Filary treści',b.pillars,'textarea')}${input('cbAvoid','Czego unikać',b.avoid,'textarea')}</div><div class="core-actions"><button class="primary" id="cbSave">Zapisz DNA marki</button></div></div></section>`}
  function stylesView(){const data=read(STYLES,[]);const base=data.length?data:[{name:'Beauty clean',format:'4:5',note:'jasne tło, produkt, czytelny nagłówek'},{name:'UGC natural',format:'9:16',note:'naturalne światło, ręce/produkt, dynamiczne kadry'},{name:'Book / review',format:'4:5',note:'okładka, detal, spokojne tło'}];return `<section class="core-local"><div class="core-card"><div class="section-head"><div><h2>Szablony stylu</h2><p class="page-subtitle">Presety działające bez generatora AI.</p></div><span class="core-pill">LOKALNE</span></div><div class="core-list">${base.map((x,i)=>`<div class="core-row"><div><b>${esc(x.name)}</b><small>${esc(x.format)} • ${esc(x.note)}</small></div><button class="ghost" data-style-use="${i}">Użyj</button></div>`).join('')}</div><div class="core-actions"><button class="primary" id="styleAdd">＋ Dodaj preset</button></div></div></section>`}
  function ideasView(){const saved=read(IDEAS,[]);const p={...defaults.profile,...read(PROFILE,{})};const topics=['3 rzeczy, których nie warto kupować w ciemno','Budżetowy zamiennik droższego produktu','Test po 7 dniach — co naprawdę się zmieniło','Ranking 3 produktów od najlepszego','Najczęstszy błąd przy wyborze produktu','Czy droższe naprawdę znaczy lepsze?','Co kupiłabym ponownie, a czego nie','Porównanie dwóch popularnych produktów'];return `<section class="core-local"><div class="core-card"><div class="section-head"><div><h2>Pomysły na treści</h2><p class="page-subtitle">Bank tematów dla ${esc(p.niche)} — bez AI.</p></div><span class="core-pill">LOCAL ENGINE</span></div><div class="core-actions"><button class="primary" id="ideasGenerate">⚡ Wygeneruj 8 pomysłów</button></div><div class="core-list" id="ideaList">${(saved.length?saved:topics).map((x,i)=>`<div class="core-row"><div><b>${esc(x)}</b><small>Hook + rozwinięcie można wygenerować w lokalnym copywriterze</small></div><button class="ghost" data-idea-save="${i}">Zapisz</button></div>`).join('')}</div></div></section>`}
  function settingsView(){const mode=localStorage.getItem('aii-mode')||'online-local';return `<section class="core-local"><div class="core-card"><div class="section-head"><div><h2>Ustawienia</h2><p class="page-subtitle">Tryb pracy i lokalne dane aplikacji.</p></div><span class="core-pill">BEZ SEKRETÓW W UI</span></div><div class="core-check"><div><b>Tryb zapisany:</b> ${esc(mode)}</div><div><b>Dane lokalne:</b> localStorage aktywne</div><div><b>AI:</b> opcjonalne — brak AI nie blokuje aplikacji</div><div><b>Offline:</b> lokalny zapis i kolejka synchronizacji</div></div><div class="core-actions"><button class="ghost" id="exportLocal">Eksportuj dane JSON</button><button class="ghost" id="backupLocal">Utwórz kopię lokalną</button></div></div></section>`}
  function supportView(){return `<section class="core-local"><div class="core-card"><div class="section-head"><div><h2>Wsparcie</h2><p class="page-subtitle">Diagnostyka bez wysyłania prywatnych tokenów.</p></div><span class="core-pill">SELF-CHECK</span></div><div class="core-help"><b>Szybka diagnostyka</b><br>1. Jeśli moduł nie pokazuje danych, użyj jego przycisku „Odśwież”.<br>2. Przy braku AI aplikacja powinna przejść na ONLINE bez AI.<br>3. Przy braku internetu dane lokalne nadal powinny być dostępne.<br>4. Tokenów i sekretów nie wpisuj w pola aplikacji ani w czat.</div><div class="core-actions"><button class="primary" id="selfCheck">Uruchom test lokalny</button></div><div class="core-list" id="selfCheckResult"></div></div></section>`}
  function render(page){const root=q('#content');if(!root)return;const map={profile:profileView,brand:brandView,styles:stylesView,ideas:ideasView,settings:settingsView,support:supportView};if(!map[page])return;root.innerHTML=map[page]();bind(page)}
  function bind(page){if(page==='profile')q('#cpSave')?.addEventListener('click',()=>{write(PROFILE,{displayName:q('#cpName').value,handle:q('#cpHandle').value,niche:q('#cpNiche').value,platform:q('#cpPlatform').value,bio:q('#cpBio').value});toast('Profil zapisany lokalnie')});if(page==='brand')q('#cbSave')?.addEventListener('click',()=>{write(BRAND,{tone:q('#cbTone').value,promise:q('#cbPromise').value,pillars:q('#cbPillars').value,avoid:q('#cbAvoid').value});toast('DNA marki zapisane')});if(page==='styles'){q('#styleAdd')?.addEventListener('click',()=>{const a=read(STYLES,[]);a.push({name:`Preset ${a.length+1}`,format:'9:16',note:'własny preset'});write(STYLES,a);render('styles')});qa('[data-style-use]').forEach(b=>b.onclick=()=>{localStorage.setItem('aii-active-style',b.dataset.styleUse);toast('Preset ustawiony jako aktywny')})}if(page==='ideas')q('#ideasGenerate')?.addEventListener('click',()=>{const n=['5 błędów, które kosztują więcej niż myślisz','Produkt tygodnia: plusy, minusy, dla kogo','Tani vs drogi — porównanie bez filtra','Jedna rzecz, którą zmieniłabym w tej rutynie','Czy warto dopłacić? szybki werdykt','Moje 3 kryteria dobrego zakupu','Najbardziej niedoceniony produkt w tej kategorii','Test: obietnica producenta vs rzeczywistość'];write(IDEAS,n);render('ideas')});if(page==='settings'){q('#backupLocal')?.addEventListener('click',()=>{localStorage.setItem('aii-backup',JSON.stringify(Object.fromEntries(Object.keys(localStorage).map(k=>[k,localStorage.getItem(k)]))));toast('Kopia lokalna utworzona')});q('#exportLocal')?.addEventListener('click',()=>{const data=JSON.stringify(Object.fromEntries(Object.keys(localStorage).map(k=>[k,localStorage.getItem(k)])),null,2),blob=new Blob([data],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='ai-influencer-studio-backup.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500)})}if(page==='support')q('#selfCheck')?.addEventListener('click',()=>{const checks=[['Przeglądarka online',navigator.onLine],['Local storage',(()=>{try{localStorage.setItem('_aii_test','1');localStorage.removeItem('_aii_test');return true}catch{return false}})()],['Content Score',!!window.AIIContentScore],['Statystyki',!!window.AIIStatsStudio],['Planer lokalny',!!window.AIILocalSocialPlanner||!!window.AIILocalPlanner]];q('#selfCheckResult').innerHTML=checks.map(([n,ok])=>`<div class="core-row"><b>${esc(n)}</b><span>${ok?'✅ OK':'⚠️ sprawdź'}</span></div>`).join('')})}
  function init(){qa('.nav-item').forEach(a=>a.addEventListener('click',()=>{const p=a.dataset.view;if(['profile','brand','styles','ideas','settings','support'].includes(p))setTimeout(()=>render(p),25)}));}
  document.addEventListener('DOMContentLoaded',init);window.AIILocalCoreViews={render};
})();