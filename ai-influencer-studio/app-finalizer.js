(() => {
  const q=(s,r=document)=>r.querySelector(s);
  const qa=(s,r=document)=>[...r.querySelectorAll(s)];
  const read=(k,d)=>{try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(d))}catch{return d}};
  const write=(k,v)=>localStorage.setItem(k,JSON.stringify(v));
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
  const keys={profile:'aii-profile-local',brand:'aii-brand-local',styles:'aii-style-presets',ideas:'aii-idea-bank'};
  const toast=t=>window.showToast?.(t);

  const css=document.createElement('style');
  css.textContent=`.final-suite{display:grid;gap:12px}.final-card{padding:14px;border:1px solid #e7e9f1;border-radius:15px;background:#fff}.final-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.final-grid-3{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px}.final-card label{display:grid;gap:5px;font-size:7px;color:#6f7683}.final-card input,.final-card textarea,.final-card select{width:100%;box-sizing:border-box;border:1px solid #e2e5ed;border-radius:9px;padding:9px;background:#fff;font:inherit}.final-row{display:flex;gap:7px;flex-wrap:wrap;align-items:center}.final-item{padding:10px;border:1px solid #eceef5;border-radius:11px;background:#fafafe}.final-item b{display:block;font-size:9px}.final-item span{display:block;font-size:7px;color:#777f8d;margin-top:4px}.final-status{display:inline-flex;padding:4px 7px;border-radius:999px;background:#e7f7ec;color:#287a4b;font-size:6.8px;font-weight:900}.final-status.warn{background:#fff3dd;color:#91691d}.final-diagnostic{display:grid;gap:6px}.final-check{display:flex;justify-content:space-between;gap:10px;padding:9px;border:1px solid #eceef5;border-radius:10px;background:#fafafe;font-size:7px}.final-idea{padding:10px;border:1px solid #eceef5;border-radius:11px;background:#fafafe}.final-idea b{font-size:8px}.final-idea p{font-size:7px;color:#6f7683;margin:4px 0 0;line-height:1.45}@media(max-width:760px){.final-grid,.final-grid-3{grid-template-columns:1fr}}`;
  document.head.appendChild(css);

  const profileDefault={name:'Karolina',handle:'@karolajna.86',niche:'Beauty / Lifestyle',bio:'Testy, UGC, budżetowe odkrycia i codzienny lifestyle',tone:'naturalny, potoczny, konkretny'};
  const brandDefault={promise:'Praktyczne rekomendacje bez zadęcia',pillars:'testy produktów, UGC, budżetowe kosmetyki, lifestyle',avoid:'przesadzone obietnice, clickbait bez wartości',cta:'Zapisz i daj znać, co testujemy dalej.'};
  const styleDefault=[{name:'Clean Beauty',mood:'jasny, miękki, produktowy'},{name:'UGC Natural',mood:'telefon, codzienne światło, autentyczność'},{name:'Dark Premium',mood:'ciemne tło, kontrast, elegancja'}];
  if(!localStorage.getItem(keys.profile))write(keys.profile,profileDefault);
  if(!localStorage.getItem(keys.brand))write(keys.brand,brandDefault);
  if(!localStorage.getItem(keys.styles))write(keys.styles,styleDefault);

  function pageActive(p){return q('.nav-item.active')?.dataset?.view===p;}
  function setMeta(title,sub){q('#pageTitle')&&(q('#pageTitle').textContent=title);q('#pageSubtitle')&&(q('#pageSubtitle').textContent=sub)}

  function profileView(){
    const p={...profileDefault,...read(keys.profile,{})};
    return `<section class="final-suite"><div class="final-card"><div class="section-head"><div><h2>Profil i wizerunek</h2><p class="page-subtitle">Lokalne dane profilu — dostępne również offline.</p></div><span class="final-status">OFFLINE READY</span></div><div class="final-grid"><label>Nazwa<input id="fpName" value="${esc(p.name)}"></label><label>Handle<input id="fpHandle" value="${esc(p.handle)}"></label><label>Nisza<input id="fpNiche" value="${esc(p.niche)}"></label><label>Ton komunikacji<input id="fpTone" value="${esc(p.tone)}"></label></div><label style="margin-top:10px">Bio<textarea id="fpBio" rows="4">${esc(p.bio)}</textarea></label><div class="final-row" style="margin-top:10px"><button class="primary" id="fpSave">Zapisz profil</button><button class="ghost" id="fpReset">Przywróć bazę</button></div></div></section>`;
  }
  function bindProfile(){q('#fpSave')?.addEventListener('click',()=>{write(keys.profile,{name:q('#fpName').value.trim(),handle:q('#fpHandle').value.trim(),niche:q('#fpNiche').value.trim(),tone:q('#fpTone').value.trim(),bio:q('#fpBio').value.trim()});toast?.('Profil zapisany lokalnie')});q('#fpReset')?.addEventListener('click',()=>{write(keys.profile,profileDefault);render('profile')})}

  function brandView(){const b={...brandDefault,...read(keys.brand,{})};return `<section class="final-suite"><div class="final-card"><div class="section-head"><div><h2>Marka osobista</h2><p class="page-subtitle">DNA marki używane przez lokalne generatory.</p></div><span class="final-status">0 TOKENÓW</span></div><div class="final-grid"><label>Obietnica marki<textarea id="fbPromise" rows="3">${esc(b.promise)}</textarea></label><label>Filary treści<textarea id="fbPillars" rows="3">${esc(b.pillars)}</textarea></label><label>Czego unikać<textarea id="fbAvoid" rows="3">${esc(b.avoid)}</textarea></label><label>Domyślne CTA<textarea id="fbCta" rows="3">${esc(b.cta)}</textarea></label></div><button class="primary" id="fbSave" style="margin-top:10px">Zapisz DNA marki</button></div></section>`}
  function bindBrand(){q('#fbSave')?.addEventListener('click',()=>{write(keys.brand,{promise:q('#fbPromise').value.trim(),pillars:q('#fbPillars').value.trim(),avoid:q('#fbAvoid').value.trim(),cta:q('#fbCta').value.trim()});toast?.('DNA marki zapisane')})}

  function stylesView(){const list=read(keys.styles,styleDefault);return `<section class="final-suite"><div class="final-card"><div class="section-head"><div><h2>Szablony stylu</h2><p class="page-subtitle">Presety wizualne zapisane na urządzeniu.</p></div><button class="primary" id="fsAdd">＋ Dodaj styl</button></div><div class="final-grid-3" id="fsList">${list.map((x,i)=>`<div class="final-item"><b>${esc(x.name)}</b><span>${esc(x.mood)}</span><button class="ghost" data-fs-del="${i}" style="margin-top:8px">Usuń</button></div>`).join('')}</div></div></section>`}
  function bindStyles(){q('#fsAdd')?.addEventListener('click',()=>{const list=read(keys.styles,styleDefault);list.push({name:`Nowy styl ${list.length+1}`,mood:'edytowalny preset lokalny'});write(keys.styles,list);render('styles')});qa('[data-fs-del]').forEach(b=>b.onclick=()=>{const list=read(keys.styles,styleDefault);list.splice(Number(b.dataset.fsDel),1);write(keys.styles,list);render('styles')})}

  function ideas(){const p=read(keys.profile,profileDefault),brand=read(keys.brand,brandDefault),formats=['Reels','Carousel','Post'];const hooks=['3 błędy, które popełniasz przy','Sprawdź zanim kupisz','Tego nikt Ci nie mówi o','Budżetowo, ale czy warto?','Test po 7 dniach:'];const topics=String(brand.pillars||p.niche).split(',').map(x=>x.trim()).filter(Boolean);const out=[];for(let i=0;i<12;i++){const topic=topics[i%Math.max(1,topics.length)]||p.niche;out.push({format:formats[i%formats.length],title:`${hooks[i%hooks.length]} ${topic}`,cta:i%2?'Napisz w komentarzu, czy chcesz część 2.':'Zapisz ten materiał na później.'})}return out}
  function ideasView(){const bank=read(keys.ideas,[]);const generated=ideas();return `<section class="final-suite"><div class="final-card"><div class="section-head"><div><h2>Pomysły na treści</h2><p class="page-subtitle">Generator regułowy bez OpenAI.</p></div><span class="final-status">0 TOKENÓW</span></div><div class="final-row"><button class="primary" id="fiGenerate">Generuj 12 pomysłów</button><button class="ghost" id="fiSaveAll">Zapisz zestaw</button></div></div><div class="final-grid" id="fiGrid">${(bank.length?bank:generated).map(x=>`<div class="final-idea"><b>${esc(x.format)} · ${esc(x.title)}</b><p>${esc(x.cta)}</p></div>`).join('')}</div></section>`}
  function bindIdeas(){q('#fiGenerate')?.addEventListener('click',()=>{write(keys.ideas,ideas());render('ideas')});q('#fiSaveAll')?.addEventListener('click',()=>{write(keys.ideas,ideas());toast?.('Bank pomysłów zapisany')})}

  function voiceView(){const v=read('aii-voice-local',{style:'Naturalny',tempo:'Średnie',rules:'krótkie zdania, język potoczny, bez przesadnych obietnic'});return `<section class="final-suite"><div class="final-card"><div class="section-head"><div><h2>Głos AI / tryb lokalny</h2><p class="page-subtitle">Profil głosu działa jako zestaw zasad także bez syntezy AI.</p></div><span class="final-status">LOCAL</span></div><div class="final-grid"><label>Styl<select id="fvStyle"><option ${v.style==='Naturalny'?'selected':''}>Naturalny</option><option ${v.style==='Ekspercki'?'selected':''}>Ekspercki</option><option ${v.style==='Humorystyczny'?'selected':''}>Humorystyczny</option></select></label><label>Tempo<select id="fvTempo"><option>Wolne</option><option ${v.tempo==='Średnie'?'selected':''}>Średnie</option><option>Szybkie</option></select></label></div><label style="margin-top:10px">Zasady<textarea id="fvRules" rows="4">${esc(v.rules)}</textarea></label><button class="primary" id="fvSave" style="margin-top:10px">Zapisz profil głosu</button></div></section>`}
  function bindVoice(){q('#fvSave')?.addEventListener('click',()=>{write('aii-voice-local',{style:q('#fvStyle').value,tempo:q('#fvTempo').value,rules:q('#fvRules').value.trim()});toast?.('Profil głosu zapisany')})}

  function settingsView(){const mode=localStorage.getItem('aii-mode')||'online-local';return `<section class="final-suite"><div class="final-card"><div class="section-head"><div><h2>Ustawienia</h2><p class="page-subtitle">Tryb pracy, kopia zapasowa i prywatność.</p></div><span class="final-status">LOCAL FIRST</span></div><div class="final-grid"><div class="final-item"><b>Tryb aplikacji</b><span>${esc(mode)}</span><div class="final-row" style="margin-top:8px"><button class="ghost" data-mode-set="online-local">ONLINE bez AI</button><button class="ghost" data-mode-set="offline">OFFLINE</button></div></div><div class="final-item"><b>Kopia danych</b><span>Eksport wszystkich kluczy aplikacji z localStorage.</span><div class="final-row" style="margin-top:8px"><button class="primary" id="fsExport">Eksport JSON</button></div></div></div></div></section>`}
  function bindSettings(){qa('[data-mode-set]').forEach(b=>b.onclick=()=>{localStorage.setItem('aii-mode',b.dataset.modeSet);window.dispatchEvent(new StorageEvent('storage',{key:'aii-mode',newValue:b.dataset.modeSet}));toast?.('Tryb zapisany');render('settings')});q('#fsExport')?.addEventListener('click',()=>{const data={};Object.keys(localStorage).filter(k=>k.startsWith('aii-')).forEach(k=>data[k]=localStorage.getItem(k));const blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`ai-influencer-studio-backup-${new Date().toISOString().slice(0,10)}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)})}

  function diagnostics(){const checks=[['LocalStorage',(()=>{try{localStorage.setItem('__aii_test','1');localStorage.removeItem('__aii_test');return true}catch{return false}})()],['Content Score',!!window.AIIContentScore],['Statystyki',!!window.AIIStatsStudio],['Trendy 7/30/90',!!window.AIIStatsTrends],['Lokalny planer',!!window.AIILocalAppCompletion||!!window.AIILocalPlanner],['Tryb offline',true],['Sieć',navigator.onLine]];return checks}
  function supportView(){const c=diagnostics();return `<section class="final-suite"><div class="final-card"><div class="section-head"><div><h2>Wsparcie i diagnostyka</h2><p class="page-subtitle">Szybki test funkcji bez wysyłania danych na zewnątrz.</p></div><span class="final-status ${c.every(x=>x[1])?'':'warn'}">${c.filter(x=>x[1]).length}/${c.length} OK</span></div><div class="final-diagnostic">${c.map(x=>`<div class="final-check"><span>${esc(x[0])}</span><b>${x[1]?'✓ OK':'⚠ sprawdź'}</b></div>`).join('')}</div><button class="ghost" id="fdRefresh" style="margin-top:10px">Uruchom ponownie test</button></div></section>`}
  function bindSupport(){q('#fdRefresh')?.addEventListener('click',()=>render('support'))}

  const views={profile:[profileView,bindProfile,'Profil i wizerunek','Lokalny profil twórcy.'],voice:[voiceView,bindVoice,'Głos AI','Profil głosu i zasady komunikacji.'],brand:[brandView,bindBrand,'Marka osobista','DNA marki i komunikacja.'],styles:[stylesView,bindStyles,'Szablony stylu','Presety wizualne.'],ideas:[ideasView,bindIdeas,'Pomysły na treści','Bank pomysłów bez tokenów.'],settings:[settingsView,bindSettings,'Ustawienia','Tryb pracy i dane lokalne.'],support:[supportView,bindSupport,'Wsparcie','Diagnostyka aplikacji.']};
  function render(page){const def=views[page];if(!def)return;const root=q('#content');if(!root)return;setMeta(def[2],def[3]);root.innerHTML=def[0]();def[1]?.()}
  function bind(){qa('.nav-item').forEach(a=>{const p=a.dataset.view;if(views[p])a.addEventListener('click',()=>setTimeout(()=>render(p),20))})}
  function repairPlaceholder(){const active=q('.nav-item.active')?.dataset?.view;if(views[active]&&/Moduł jest przygotowany do dalszego rozwijania/i.test(q('#content')?.textContent||''))render(active)}
  document.addEventListener('DOMContentLoaded',()=>{bind();const root=q('#content');if(root)new MutationObserver(()=>setTimeout(repairPlaceholder,10)).observe(root,{childList:true,subtree:false});setTimeout(repairPlaceholder,300)});
  window.AIIAppFinalizer={render,diagnostics,ideas};
})();